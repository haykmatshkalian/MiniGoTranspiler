#include <stdio.h>

int main() {
    int x = (5 + (3 * 2));
    printf("%d\n", x);
    if ((x >= 10)) {
        printf("%d\n", x);
    }
    while ((x < 3)) {
        printf("%d\n", x);
    }

    return 0;
}
